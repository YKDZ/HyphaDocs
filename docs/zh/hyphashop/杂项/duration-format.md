# 时长字符串

时长字符串用于提高配置中与时间相关的条目的可读性。

该字符串由若干 `数字 + 单位` 组成，常见的写法如下：

```yaml
period: 24h
period: 20s
period: 0.5s
# 多个时间组合必须用空格分隔
period: 1h 20m 30s
```

可用的单位如下：

- **s**：秒
- **m**：分钟
- **h**：小时
- **d**：天
- **w**：周

任何错误的时间格式在被解析时都会在控制台输出一条警告并被视作 `0s`。
